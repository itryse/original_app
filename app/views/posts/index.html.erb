<main class="main">
  <head>
    <meta charset="UTF-8">
    <title>オリジナルアプリ</title>
  </head>
  <body>
  <%# <%= image_tag "dqwalk.jpeg", class:"list-pict" %>
    <div class="inner">
      <% if user_signed_in? %>
        <div class="blackboard">
          <p><%= "＊「こんにちは、ゆうしゃ #{current_user.nickname}さん。」" %></p>
        </div>
      <% else %>
        <div class="blackboard">
          <p>＊「こんにちは。なにをしますか？」</p>
        </div>
      <% end %>
        <div class="blackboard answer float-r">
          <% if user_signed_in? %>
            <p><%= link_to "ちゅうだんする", destroy_user_session_path, method: :delete , class:"blink-before" %></p>
            <p><%= link_to "とうこうする", new_post_path, class:"blink-before" %></p>
          <% else %>
            <p><%= link_to "ぼうけんをはじめる", new_user_registration_path, class:"blink-before" %></p>
            <p><%= link_to "つづきからはじめる", new_user_session_path, class:"blink-before" %></p>
          <% end %>
        </div>
    </div>
    <%# <投稿情報はここに表示> %>
    <ul class="item-lists">
      <li class="item">
        <%= render partial: "post" , collection: @posts %>
      </li>
    </ul>
    <%# <投稿情報はここに表示> %>
  </body>
</main>

      <%# <ul class='item-lists'>
        <% @posts.each do |product| %>
          <%# <li class='list'>
            <div class='item-img-content'>
              <%= image_tag post.image, class: "item-img" %>

            <%# </div>
            <div class='item-info'>
              <h3 class='item-name'>
                <%= post.title %>
              <%# </h3>
              <div class='item-price'>
                <span><%= post.today_comment.name<br><%= post.today_comment.name %>
                <%# <div class='star-btn'>
                  <%= image_tag "star.png", class:"star-icon" %>
                  <%# <span class='star-count'>0</span>
                </div>
              </div>
            </div>
          </li>
        <% end %>
      <%# </ul> %>